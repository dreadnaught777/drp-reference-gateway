# Benchmarks

Any latency or throughput number cited in the README or docs must name a
reproducible script here (build brief section 5, criterion 5; Suite L). No
numbers are claimed yet, so there are no scripts here yet.
